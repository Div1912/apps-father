import { Bot } from "grammy";
import { BotContext } from "../../types";

export function registerPhotoHandlers(_bot: Bot<BotContext>) {
  // Photo handlers removed — file uploads handled via Mini App now
}
