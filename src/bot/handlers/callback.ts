import { Bot } from "grammy";
import { BotContext } from "../../types";

export function registerCallbackHandlers(_bot: Bot<BotContext>) {
  // All callback handlers removed — Mini App API handles everything now
}
