import { Bot } from "grammy";
import { BotContext } from "../../types";

export function registerConversationHandlers(_bot: Bot<BotContext>) {
  // All conversation handlers removed — Mini App API handles everything now
}
