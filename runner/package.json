{
  "name": "apps-father-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Per-project worker runtime baked into the apps-father-runner Docker image. Provides core deps (express, ws, dotenv, better-sqlite3, grammy) so the worker entry boots in any container without a per-project install. User-declared packages live in /workspace/node_modules and are layered on top via NODE_PATH.",
  "main": "worker-entry.js",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "grammy": "^1.30.0",
    "ws": "^8.18.0"
  }
}
