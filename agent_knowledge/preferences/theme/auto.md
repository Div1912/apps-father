THEME = Auto.

- Read `Telegram.WebApp.colorScheme` on init AND subscribe to `themeChanged` to swap palette live.
- Apply the style's light palette when `colorScheme === 'light'` and the dark palette otherwise.
