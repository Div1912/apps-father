BOTTOM NAV = Action grid.

- The Home screen is a 2x2 (or 2x3) grid of large square action cards (one per main feature). No persistent bottom bar.
- Sub-screens use a back button in the header to return Home; deep-linking always goes through Home.
