BOTTOM NAV = Floating CTA only.

- Do NOT render a tab bar. Use a single floating action button bottom-right (24px from edges) for the main action.
- Secondary navigation lives inside the header (back button + section switcher) or as inline tabs at the top of each screen.
