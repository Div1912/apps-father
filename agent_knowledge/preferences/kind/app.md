KIND = App.

- Build a standard Telegram Mini App: HTML/CSS/JS frontend in `mini_app/`, optional `routes.js` backend, optional bot integration.
- Follow the rest of the PREFERENCES block (Style, Theme, Header, Density, Bottom menu) and the standard `workflow-new.md` build flow.
- This is the default when Kind is left on Auto.
