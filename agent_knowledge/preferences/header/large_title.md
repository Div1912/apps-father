HEADER = Large title.

- Two-line area at the top of every screen: small overline (page category) + display-size page title (28-32px).
- Collapse to a compact 48px header on scroll (use IntersectionObserver or scroll listener).
- Do not use this layout on full-screen modal flows (checkout, onboarding step) — fall back to Minimal there.
