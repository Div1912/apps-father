STYLE = Brutalist.

- Palette: background `#f0f0f0`, surface `#e0e0e0`, text `#000000`, muted `#666666`, primary `#000000` (text `#ffe600`), accent `#ffe600`, border `#000000`.
- Typography: Unbounded for everything — weights 700/900. Tight letter-spacing on headlines (-0.5 to -3px). Big sizes (hero number 48–72px). No optical kerning tricks.
- Geometry: 0px corner radius EVERYWHERE. 2–3px solid black borders. Box-shadow only as hard offset: `4px 4px 0 #ffe600` (no blur).
- Tone: confident, anti-trend. Every element is visible and intentional. Copy is blunt ("No fluff. Just results.", "Connect Now", "→ Action").
- Special elements: ticker-tape accent bar in `#ffe600`. Grid cells with hard 2px black dividers. One yellow cell per data grid for visual weight. Stamp-like uppercase labels with letter-spacing 1.5px+.
- Forbidden: round corners, gradients, blurred shadows, glassmorphism, soft transitions, light typography weights. Glassmorphism is the enemy.
