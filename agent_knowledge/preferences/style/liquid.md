STYLE = Liquid.

- Palette: background `#03080f`, text `rgba(255,255,255,0.9)`, muted `rgba(255,255,255,0.2)`, primary `#00c8ff` (cyan), accent `#00ffb4` (mint), border `rgba(0,200,255,0.08)`.
- Typography: Space Grotesk for everything. Hero numbers: 26–40px, weight 600/700, letter-spacing -1px.
- Background: 2 absolute "blob" divs with `border-radius: 60% 40% 50% 70% / 50% 60% 40% 50%`, `filter: blur(25–30px)`, and a CSS keyframe animation morphing between random border-radius values over 8–10s (`ease-in-out infinite`).
- Geometry: 16–24px corner radius. Conic-gradient rings for progress (cyan→mint→cyan). Pills with `border-radius: 20px` and translucent cyan backgrounds.
- Glow: every primary surface has `box-shadow: 0 0 30px rgba(0,200,255,0.08)` plus inset highlight `inset 0 1px 0 rgba(0,200,255,0.1)`.
- Tone: living, breathing, organic. Health trackers, system monitors, biotech.
- Forbidden: sharp angles, static backgrounds, warm tones, any yellow/orange/red. Cold cyan-mint spectrum only.
