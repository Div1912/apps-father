STYLE = Crypto.

- Palette: background `#0b0f17`, surface `#111827`, text `#e5e7eb`, muted `#94a3b8`, primary `#10b981` (dark text `#04130c`), accent `#f59e0b`, border `#1f2937`.
- Typography: Space Grotesk for headings, JetBrains Mono for numeric values, balances, addresses, transaction ids.
- All monetary values, percentages and counters MUST render in monospace. Up = `#10b981`, down = `#ef4444`.
- Geometry: 10px corner radius, dense card layouts with hairline borders. No playful illustrations.
- Surfaces: subtle inner shadow `inset 0 1px 0 rgba(255,255,255,0.04)` on cards to fake light from above.
