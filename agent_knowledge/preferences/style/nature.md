STYLE = Nature.

- Palette: background `#f0fdf4`, surface `#ffffff`, text `#14532d`, muted `#4b7c5a`, primary `#16a34a` (white text), accent `#4ade80`, border `#bbf7d0`.
- Typography: DM Sans for body. Optional Fraunces for display titles. Rounded, organic feel.
- Geometry: 12–20px corner radius. Hero blocks use soft green gradients (`linear-gradient(135deg, #dcfce7, #bbf7d0)`). Pill-shaped CTAs allowed (border-radius 20px+).
- Tone: calm, responsible, optimistic. Nature emoji sparingly in headers (`🌿` `🌱` `♻️`).
- Forbidden: dark backgrounds, neon greens, harsh contrasts, glassmorphism. Palette stays in green-sage family only.
